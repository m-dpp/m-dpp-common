[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "m-dpp-common"
version = "0.10.1"
description = "Shared infrastructure for the mDPP services: GS1 identifiers, DB plumbing, ORM mixins, auth seam (subjects, memberships, principal resolution), RBAC engine, Organisation entity. Infrastructure only — no domain logic."
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "sqlalchemy[asyncio]",
    "pydantic",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
